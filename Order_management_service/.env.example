# Copy to .env for local tools only — Cursor MCP reads the *system* env var
# GITHUB_PERSONAL_ACCESS_TOKEN (set in Windows User env or shell profile).
# Do not commit a real token.
GITHUB_PERSONAL_ACCESS_TOKEN=
